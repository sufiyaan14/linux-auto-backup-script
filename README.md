# Linux Automated Backup Script

A Bash script that automatically creates compressed backups of a directory.

## Features
- Automatic folder backup
- Creates .tar.gz compressed archive
- Adds date to backup file
- Creates backup folder if it doesn't exist

## Screenshot


## Run Script

chmod +x backup.sh

./backup.sh
